# OpenClaw 抖音自动化 Skill（openclaw_use_skill）

这个仓库用于分享可复用的 OpenClaw 技能，让其他开发者把同样的抖音自动化流程快速迁移到自己的智能体。

## 核心目标

让你的 OpenClaw 学会：

- 抖音创作者后台图文发布
- AI 配图 + 封面同步
- 配乐选择（悬停歌曲行 → 点击“使用”）
- 发布后在内容管理页做入库核验
- 评论区自动回复（v2：意图识别 + 安全护栏）

## Skill 位置

- 主技能目录：`skills/douyin-automation/`
- 对外分发套件（可选）：`openclaw-douyin-skill-kit/skills/douyin-automation/`

## 如何给你的 OpenClaw 安装

把仓库中的 skill 复制到你自己的 OpenClaw 工作区：

```bash
mkdir -p ~/.openclaw/workspace/skills
cp -R skills/douyin-automation ~/.openclaw/workspace/skills/
```

然后重启/刷新 OpenClaw 会话。

## 智能体配置说明（让它“看仓库就会配”）

请给你的 OpenClaw 智能体补充以下配置（最少集）：

1. **启用浏览器自动化能力**（`browser` 工具）
2. **工作区包含** `~/.openclaw/workspace/skills/douyin-automation`
3. 智能体系统指令中明确：
   - 发文流程固定为：标题 → 摘要 → 正文 → AI配图 → 配乐 → 发布 → 管理页核验
   - 配乐必须执行“悬停歌曲行后点击使用”
   - 正文不少于100字
   - 发布后必须到内容管理页确认入库

### 可直接复制给智能体的指令模板

```text
使用 skills/douyin-automation/SKILL.md 作为抖音自动化唯一执行标准。
执行图文发布时，严格按以下顺序：
1) 填标题与摘要
2) 填正文（不少于100字）
3) 生成AI头图并确认封面
4) 打开配乐弹窗，先悬停歌曲行，再点右侧“使用”
5) 关闭弹窗后确认主表单出现曲名且按钮为“修改音乐”
6) 点击发布
7) 跳转内容管理页核验作品是否出现在“已发布”或“审核中”
若任一步失败，记录失败原因并重试，不要跳过核验。
```

### 开发者侧配置检查清单

- [ ] OpenClaw 已启用 `browser` 工具
- [ ] 账号已在 `creator.douyin.com` 完成登录
- [ ] `skills/douyin-automation/SKILL.md` 可被智能体读取
- [ ] 已允许人工介入验证码/风控步骤
- [ ] 有发布后的核验回报（标题、时间、状态）

## 给开发者的使用说明（可直接转发）

把这个仓库链接发给开发者，让他们对自己的 OpenClaw 说：

- “请使用 `douyin-automation` skill 执行抖音图文自动发布流程。”
- “发文前先选音乐，按悬停歌曲行后点击使用的规则执行。”
- “发布后去内容管理页核验是否入库。”

## 已验证关键规则

1. 正文建议 **≥100字**，否则可能出现“点击发布但未入库”。
2. 配乐必须按可见交互：**悬停歌曲行 → 点击右侧“使用”**。
3. 配乐成功判定：主表单出现曲名且按钮变为 **“修改音乐”**。
4. 发布成功判定：内容管理页能看到新作品（已发布/审核中）。

## 免责声明

请遵守抖音平台规则与当地法律，控制自动化频率，优先人工监督与低风险操作。